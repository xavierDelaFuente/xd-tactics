services:
  postgres:
    image: postgres:18
    environment:
      POSTGRES_USER: xd_tactics
      POSTGRES_PASSWORD: xd_tactics
      POSTGRES_DB: xd_tactics
    ports:
      - '5432:5432'
    volumes:
      - postgres-data:/var/lib/postgresql

volumes:
  postgres-data:
